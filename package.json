{
  "name": "strava-visualize",
  "version": "0.1.0",
  "private": true,
  "description": "A local web dashboard for visualizing Strava running trends over time.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/server.test.js tests/worker.test.js",
    "test:e2e": "playwright test",
    "deploy": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy public --project-name strava-visualize"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
